[package]
name = "jobs"
version = "0.0.1"
authors = ["John Kåre Alsaker <john.kare.alsaker@gmail.com>"]
description = "build system"
license = "MIT"

[dependencies]
jobs-proc-macro = { path = "jobs-proc-macro", version = "0.0.1" }
lazy_static = "1.0.0"